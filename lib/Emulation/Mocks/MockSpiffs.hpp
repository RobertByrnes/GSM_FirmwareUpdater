#if not defined(MOCK_SPIFFS_H)
#define MOCK_SPIFFS_H

#include <Arduino.h>
#include <Emulator.h>


class MockSpiffs : public Emulator {
public:
	MockSpiffs() {}
	~MockSpiffs() {}


protected:

private:

};

#endif

